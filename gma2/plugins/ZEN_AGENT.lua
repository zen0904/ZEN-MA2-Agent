-- ZEN Agent adapter placeholder for grandMA2.
-- Future scope: expose show-object information (Layout / Group / Fixture) to
-- the portable Agent. It intentionally contains no AI or autonomous command
-- execution logic; the Agent remains responsible for preview and safety gates.

local function main()
    Echo("ZEN_AGENT adapter scaffold loaded")
end

return main
