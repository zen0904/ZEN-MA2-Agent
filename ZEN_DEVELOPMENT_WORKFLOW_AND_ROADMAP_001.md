# ZEN Development Workflow + Roadmap 001

## Why this control layer exists

The project now discovers research directions and implementation opportunities
faster than each can be safely validated. This control layer ensures a finding
is classified and gated before it changes the mainline. GitHub/repository
history remains the durable source of truth; this work adds a concise way to
recover the active objective and deferred discoveries without replaying chat
history.

## Durable control artifacts

- [`docs/ZEN_DEVELOPMENT_WORKFLOW.md`](docs/ZEN_DEVELOPMENT_WORKFLOW.md):
  development loop, classifications, scope gate, task selection, and
  continuation behavior.
- [`docs/ZEN_PRODUCT_ROADMAP.md`](docs/ZEN_PRODUCT_ROADMAP.md): milestone
  acceptance model, product boundaries, ongoing learning tracks, and current
  mainline.
- [`data/zen_project_control.json`](data/zen_project_control.json): compact,
  machine-readable current state and seeded discovery register.
- [`AGENTS.md`](AGENTS.md): now routes continuations through the control state
  as well as the product/status documents.

## Current project position

`M1 — DESIGNER FOUNDATION` has reached the bounded exit needed for real
expressive experiments: B3 Design Intent is traceable, human-reviewable, and
does not fabricate unavailable song facts; External Knowledge Pack 001 remains
shadow-only and provenance-bearing.

`M2 — EXPRESSIVE CAPABILITY` is now active. `REAL_SONG_EXISTING_SHOW_AB_002`
proved a real typed `SET_DIMMER` density action delta using case-scoped,
Show-bound eligibility. It did **not** establish an artistically acceptable
Existing Show composition: its final equal-level cohort reports
`FINAL_COHORT_SATURATION_RISK` and
`GROUP_LEVEL_HOMOGENEITY_UNRESOLVED`, with human review still `UNSET`.

The new Current Show visual-relationship intake is valid optional case
evidence, not a prerequisite or mandatory survey. Its unknown state must not
be converted into an invented hierarchy or a blocker until a concrete case
requires those facts.

## Active gate and exact next mainline task

**Closed human review:** A/B 002 is
`TECHNICAL_EXPRESSIVE_PATH_PROVEN`, but
`ARTISTIC_GENERALIZATION_NOT_APPLICABLE`. The neutral allocator is
`EXPERIMENTAL_ONLY` and Density remains a context-dependent design dimension;
the review does not approve its numeric tie-breaking, equal levels, or
full-cohort saturation as a design strategy.

**Closed Color evidence gate:**
`SHOW_BOUND_COLOR_PRESET_APPLICABILITY_VERIFICATION_001` is `UNSUPPORTED`.
Fresh, fingerprint-matched `List Preset All` returned five Focus rows only;
`List Preset Color` returned native `Error #14: OBJECT DOES NOT EXIST`.
Fixture COLOR channel capability and generic typed `CALL_PRESET` syntax do not
create a Show-bound Color resource or prove applicability.

**Recommended next task:**
`COLOR RESOURCE PROVISIONING BOUNDARY REVIEW 001`.

That bounded architecture/human-workflow review should decide which future
Color-resource provenance paths may be considered. It must not create a
Preset, select a palette, assign a Group role, infer visual relationships, or
change a Show.

## Milestone and learning policy summary

The roadmap contains M1 Design Foundation, M2 Expressive Capability, M3
Show/Rig Adaptation, M4 Professional MA2 Implementation, M5 Focus Execute, and
M6 Production Validation. “Works but messy” fails M4; Operator Handover and
low `TIME_TO_REFINEMENT` are required at M5; no single A/B result can satisfy
M6.

Industry learning, fixture-market review (roughly every 120 days), MA/console
learning, and MA3 architecture awareness are ongoing tracks. They are not
automatic interruptions. Fixture learning teaches affordances, never automatic
fixture selection or artistic identity. Design knowledge supplies a broad
professional prior; current Show/venue facts constrain; Song/Performance
context informs intent; Zen preference personalizes among valid options.

## Scope and safety

This is project-control documentation/data only. It adds no Designer,
knowledge, capability, mapping, geometry, console, or MA2 behavior.

- Production Designer: `UNCHANGED`.
- B3: `GUIDANCE_ASSISTED_AB_ONLY`.
- A/B 002 actions: `UNCHANGED`.
- MA2 objects modified: `NONE`; MA2 write audit: `ZERO_WRITES`.
- Fixture 9999: untouched.
- `ZEN_STYLE_PROFILE`: `DEFERRED`.
- DoneAudit: `DEFERRED`.
- MA3 Builder: not started.
